import { EthereumAddress } from '@lens-protocol/shared-kernel';
import { LensConfig } from '../../consts/config';
import { Credentials } from './Credentials';
export declare class AuthenticationApi {
    private readonly sdk;
    constructor(config: LensConfig);
    challenge(address: EthereumAddress): Promise<string>;
    verify(accessToken: string): Promise<boolean>;
    authenticate(address: EthereumAddress, signature: string): Promise<Credentials>;
    refresh(refreshToken: string): Promise<Credentials>;
}
