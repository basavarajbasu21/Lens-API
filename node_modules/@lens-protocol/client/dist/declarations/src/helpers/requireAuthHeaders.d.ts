import { PromiseResult } from '@lens-protocol/shared-kernel';
import type { Authentication } from '../authentication';
import { CredentialsExpiredError, NotAuthenticatedError } from '../consts/errors';
type Handler<Val> = (headers: Record<string, string>) => Promise<Val>;
export declare function requireAuthHeaders<Val>(authentication: Authentication | undefined, handler: Handler<Val>): PromiseResult<Val, CredentialsExpiredError | NotAuthenticatedError>;
export {};
