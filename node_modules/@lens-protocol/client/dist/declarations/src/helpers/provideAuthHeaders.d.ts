import type { Authentication } from '../authentication';
type Handler<Val> = (headers: Record<string, string>) => Promise<Val>;
export declare function provideAuthHeaders<Val>(authentication: Authentication | undefined, handler: Handler<Val>): Promise<Val>;
export {};
