import { LensConfig } from '../consts/config';
import { GlobalProtocolStatsRequest } from '../graphql/types.generated';
import { GlobalProtocolStatsFragment } from './graphql/stats.generated';
/**
 * Protocol stats.
 *
 * @group LensClient Modules
 */
export declare class Stats {
    private readonly sdk;
    constructor(config: LensConfig);
    /**
     * Fetch protocol stats.
     *
     * @param request - Request object for the query
     * @returns Protocol stats
     *
     * @example
     * ```ts
     * const result = await client.stats.fetch();
     * ```
     */
    fetch(request?: GlobalProtocolStatsRequest): Promise<GlobalProtocolStatsFragment>;
}
