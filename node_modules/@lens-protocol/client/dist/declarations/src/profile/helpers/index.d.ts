/**
 * Checks if a profile handle is valid.
 *
 * @param handle - profile handle to check
 * @returns true if the handle is valid
 */
export declare function isValidProfileHandle(handle: string): boolean;
